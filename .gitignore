node_modules/
.DS_Store
.env
.env.local
dist/
build/
.idea/
.vscode/settings.json
*.log

# Real contact submissions — personal data. Never in the repo: the repo
# is public (GitHub Pages), so anything committed here is public too.
data/submissions.json
